import { EventEmitter } from '@angular/core';
import { ControlValueAccessor } from "@angular/forms";
import { AlertController, ToastController } from "ionic-angular";
export declare class TagsInputComponent implements ControlValueAccessor {
    private alertCtrl;
    private toastCtrl;
    onTagAdded: EventEmitter<any>;
    onTagRemoved: EventEmitter<any>;
    onTagAddClicked: EventEmitter<any>;
    maxTags: number;
    buttonLabel: string;
    alertTitleLabel: string;
    alertInputPlaceholder: string;
    alertButtonLabel: string;
    wordLengthRestrictionMsg: string;
    duplicatesRestrictionMsg: string;
    maxWordLength: number;
    allowDuplicates: boolean;
    private onTouchedCallback;
    private onChangeCallback;
    values: Array<any>;
    isDisabled: boolean;
    constructor(alertCtrl: AlertController, toastCtrl: ToastController);
    writeValue(value: Array<any>): void;
    registerOnChange(fn: (_: Array<any>) => void): void;
    registerOnTouched(fn: () => void): void;
    setDisabledState(isDisabled: boolean): void;
    notifyDataChanged(data: any): boolean;
    addItem(): void;
    private checkMaxItemsRestriction(index);
    private addValue(data);
    private checkDuplicatesRestriction(name);
    private checkMaxWordLengthRestriction(name);
    removeItem(index: number): void;
}
